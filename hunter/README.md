# Hunter builds

Hunter build collection for the Monument of Triumph sandbox. Mirror the layout of [`../jared/`](../jared/) (the Warlock reference): put individual builds in `builds/`, one markdown file each, using the template in [`../CONTRIBUTING.md`](../CONTRIBUTING.md).

Shared mechanics (artifact perks, stat system, keyword behavior) live in [`../general/`](../general/) — read those first, then bring your own Hunter aspects and exotics.

_Nothing here yet — add the first build in `builds/`._
